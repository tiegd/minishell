## Parsing

Checker toutes les erreurs de syntaxes pour remettre un nouveau promt avec la bonne erreur.

"Diviser" le parsing en fonction de si on a un pipe ou pas.

Remplacer ce qu'a entré l'utilisateur par une string plus simple si par exemple il y a beaucoup de quote ou qu'il y en a en trop etc.. 
Comme par exemple "e"ch'o' "quelque chose" cela doit marcher.

Il faut tokeniser chaque chose qui sont entré dans le prompt.
Par exemple dans une liste chainé mettre dans l'ordre chaques chose qu'on recoit et qui pourront être envoyé à l'execution.

