GIT :

git add -A : L’option -A sert à ajouter tous les changements dans l’arborescence de travail (working directory).

ajouter une branch : Git branch "nom de la branch".

se rendre sur la branch : git checkout "nom de la branch".

pour envoyer la branch sur le depot en ligne : git push --set-upstream origin "nom de la branch".

git fecth : recupere les données distante sans fusionner.

git pull : recupere les données distante et fusionne avec les donnée local.

git merge : pour fusionner les 2 branches vers une seule.